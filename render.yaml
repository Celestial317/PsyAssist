services:
  - type: web
    name: psyassist
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --preload --timeout 90"
    autoDeploy: true

